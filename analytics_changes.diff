diff --git a/app/admin/analytics/page.tsx b/app/admin/analytics/page.tsx
index fe38d48..7a04cb0 100644
--- a/app/admin/analytics/page.tsx
+++ b/app/admin/analytics/page.tsx
@@ -132,9 +132,12 @@ interface AnalyticsData {
   eventosMasComunes: { evento: string; count: number }[]
 }
 
+type TabType = 'general' | 'areas' | 'usuarios' | 'rutas' | 'vehiculos' | 'engagement' | 'tendencias'
+
 export default function AdminAnalyticsPage() {
   const [analytics, setAnalytics] = useState<AnalyticsData | null>(null)
   const [loading, setLoading] = useState(true)
+  const [activeTab, setActiveTab] = useState<TabType>('general')
   const router = useRouter()
 
   useEffect(() => {
@@ -995,6 +998,85 @@ export default function AdminAnalyticsPage() {
       </header>
 
       <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
+        {/* Sistema de Tabs/Navegación por Secciones */}
+        <div className="bg-white rounded-xl shadow mb-8 sticky top-0 z-10">
+          <div className="flex items-center gap-2 p-2 overflow-x-auto">
+            <button
+              onClick={() => setActiveTab('general')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'general'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              📊 General
+            </button>
+            <button
+              onClick={() => setActiveTab('areas')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'areas'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              🗺️ Áreas
+            </button>
+            <button
+              onClick={() => setActiveTab('usuarios')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'usuarios'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              👥 Usuarios
+            </button>
+            <button
+              onClick={() => setActiveTab('rutas')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'rutas'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              🗺️ Rutas
+            </button>
+            <button
+              onClick={() => setActiveTab('vehiculos')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'vehiculos'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              🚐 Vehículos
+            </button>
+            <button
+              onClick={() => setActiveTab('engagement')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'engagement'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              📈 Engagement
+            </button>
+            <button
+              onClick={() => setActiveTab('tendencias')}
+              className={`px-6 py-3 rounded-lg font-semibold text-sm transition-all whitespace-nowrap ${
+                activeTab === 'tendencias'
+                  ? 'bg-gradient-to-r from-sky-500 to-blue-600 text-white shadow-md'
+                  : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
+              }`}
+            >
+              📊 Tendencias
+            </button>
+          </div>
+        </div>
+
+        {/* KPIs Principales - Visible en todas las tabs */}
+        {activeTab === 'general' && (
+          <>
         {/* KPIs Principales */}
         <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
           <div className="bg-white rounded-xl shadow p-6">
