version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - npm --version
        - npm ci
    build:
      commands:
        - echo "Building Next.js app..."
        - echo "Checking environment variables..."
        - echo "OPENAI_API_KEY=${OPENAI_API_KEY:0:10}..."
        - echo "SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:0:10}..."
        - echo "NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}"
        - npm run build
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
  environment:
    variables:
      NODE_ENV: production
  customHeaders:
    - pattern: '/api/**'
      headers:
        - key: 'Cache-Control'
          value: 'no-store, no-cache, must-revalidate'
        - key: 'Content-Type'
          value: 'application/json'

# Variables de entorno requeridas (configurar en AWS Amplify Console):
# CRITICAL (Chatbot):
# - OPENAI_API_KEY
# - SUPABASE_SERVICE_ROLE_KEY (o NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY)
# - GOOGLE_MAPS_API_KEY
#
# OPTIONAL (Rate Limiting - FIX #2):
# - UPSTASH_REDIS_REST_URL
# - UPSTASH_REDIS_REST_TOKEN
#
# Already configured (verificar):
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
# - NEXT_PUBLIC_GOOGLE_PLACES_API_KEY
